# pipeline
pymupdf>=1.24          # PDF text, blocks, drawing rects, page rendering
openpyxl>=3.1          # xlsx sheets, cells, formulas
watchdog>=4.0          # inbox FSEvents watcher
pydantic>=2.7          # extractor output contracts

# storage
psycopg[binary]>=3.2
pgvector>=0.3
boto3>=1.34            # Cloudflare R2 (S3-compatible)

# local models — nothing leaves the machine
sentence-transformers>=3.0   # bge-small-en-v1.5 -> vector(384)

# also required, installed outside pip:
#   brew install rclone gitleaks tesseract
#   ollama pull qwen2-vl        (captions for image-only pages)
